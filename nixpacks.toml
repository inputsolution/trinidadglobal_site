# Configuración de build para Railway (Nixpacks).
# Fija Node 20 para que el build sea reproducible.

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
